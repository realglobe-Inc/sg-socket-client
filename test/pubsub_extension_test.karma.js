/**
 * Test case for pubsubExtension.
 * Runs with karma.
 */
'use strict'

const pubsubExtension = require('../shim/browser/extensions/pubsub_extension.js')
const assert = require('assert')
const co = require('co')

describe('pubsub-extension', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Pubsub extension', () => co(function * () {

  }))
})

/* global describe, before, after, it */
