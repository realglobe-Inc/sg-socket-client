/**
 * Test case for subscribeExtension.
 * Runs with mocha.
 */
'use strict'

const subscribeExtension = require('../lib/extensions/subscribe_extension.js')
const assert = require('assert')
const co = require('co')

describe('subscribe-extension', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Subscribe extension', () => co(function * () {

  }))
})

/* global describe, before, after, it */
