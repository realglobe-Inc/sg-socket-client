/**
 * Test case for pubsubExtension.
 * Runs with mocha.
 */
'use strict'

const pubsubExtension = require('../lib/extensions/pubsub_extension.js')
const assert = require('assert')
const co = require('co')

describe('pubsub-extension', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Pubsub extension', () => co(function * () {

  }))
})

/* global describe, before, after, it */
